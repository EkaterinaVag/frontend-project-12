{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "React project. Slack",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "start-server -s ./frontend/build",
    "build": "npm run build --prefix frontend"
  },
  "author": "Ekaterina Vaganova",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^2.0.3"
  }
}
